RUN: 

```bash
git clone https://github.com/kohi9noor/next-distdir-repro.git
cd next-distdir-repro
```

open the repo in dev container. 

run inside apps/web

```bash
    bun run build
```

The note will be gone permanently
